{
  "name": "limon-fez",
  "version": "0.1.2",
  "description": "Generate a random, nonsense phrase that rhymes with an input string",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "git@github.com:ducktrshessami/limon-fez.git",
  "author": "ducktrshessami",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:main && yarn build:test",
    "build:main": "tsup",
    "build:test": "tsc",
    "pretest": "yarn build",
    "test": "mocha"
  },
  "exports": {
    "types": "./dist/index.d.ts",
    "require": "./dist/index.js",
    "import": "./dist/index.mjs"
  },
  "dependencies": {
    "node-cmudict": "ducktrshessami/node-cmudict#v2.1.0"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.8",
    "mocha": "^10.7.3",
    "tsup": "^8.2.4",
    "typescript": "^5.5.4"
  }
}
